# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Use npm (package-lock.json) only — a stale bun.lockb breaks Railpack deploys
bun.lockb

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Lockfile untracked: npm cross-platform optional-deps bug breaks 'npm ci' on Linux;
# Railpack uses 'npm install' when no lockfile is present.
package-lock.json
